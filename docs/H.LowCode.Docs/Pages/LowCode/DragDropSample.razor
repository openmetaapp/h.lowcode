@page "/lowcode/dragdropsample"

<DocList></DocList>
<SlugList SlugItems="@slugItems"></SlugList>
<Content>
    <Markdown FileName="DragDropSample.md" Type="typeof(DocList)"></Markdown>
    <CodeBox>
        @Code
    </CodeBox>
    <Markdown>
        <p>
            拖拽示例
        </p>
    </Markdown>
    <DocFooter></DocFooter>
</Content>

@code {
    private IList<SlugItem> slugItems = new List<SlugItem>()
    {
        new SlugItem(){ Path="#simple", Title="简单拖拽" },
        new SlugItem(){ Path="#move", Title="移动拖拽" },
        new SlugItem(){ Path="#levitation", Title="拖动悬停效果" },
        new SlugItem(){ Path="#sort", Title="移动排序拖拽" },
        new SlugItem(){ Path="#copy", Title="复制拖拽" },
        new SlugItem(){ Path="#container", Title="容器嵌套" },
        new SlugItem(){ Path="#containermove", Title="同级容器移动" },
        new SlugItem(){ Path="#containermove2", Title="嵌套容器移动" },
        new SlugItem(){ Path="#anywhere", Title="任意位置停放" },
        new SlugItem(){ Path="#iframe", Title="iframe" }
    };

    private string Code = "Code - 拖拽示例";
}